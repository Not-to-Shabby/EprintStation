# Makefile for E-Print Station (React + Vite)
# Usage: make <target>

install:
	# Install all dependencies
	npm install

develop:
	# Start the development server
	npm run dev

build:
	# Build the project for production
	npm run build

preview:
	# Preview the production build
	npm run preview

lint:
	# Run ESLint
	npm run lint
